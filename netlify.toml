[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redirects (optional): ensure SPA routes fallback to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
